{
  "name": "okjike-ui",
  "version": "0.0.1",
  "description": "完善即刻网页版的体验。",
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "entrypoints/*"
  ],
  "scripts": {
    "build": "bun run ./index.ts",
    "dev:popup": "cd ./entrypoints/popup && bun dev",
    "preview:chrome": "cd ./dist/chrome && bun x http-server"
  },
  "dependencies": {
    "adm-zip": "^0.5.14",
    "fs-extra": "^11.2.0",
    "node-cmd": "^5.0.0",
    "@qzda/prolog": "latest"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/chrome": "^0.0.268",
    "@types/fs-extra": "^v11.0.4"
  }
}
